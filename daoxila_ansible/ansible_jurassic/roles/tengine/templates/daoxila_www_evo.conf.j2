upstream daoxila_www_evo {
{% for host in groups.daoxila_www_evo %}
    server {{ host }}:80 weight={{ weight|default(1) }};
{% endfor %}
    check interval=5000 rise=2 fall=3 timeout=1000 port=80;
}
